import styled from "styled-components";

export const style = {
    textTransform: "none",
    fontSize: 17,
}

export const StyleProfileAvatar = styled.div`
    background-color: ${props => props.theme.title};
`

