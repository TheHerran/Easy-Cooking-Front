import styled from "styled-components";
import { IconButton } from '@mui/material';

export const StyleIconButton = styled(IconButton)`
    .lightIcon {
        color: #ffd000;
    }

    .darkIcon {
        color: #000000;
    }

`

export const ThemeSwitchSpot = styled.div`
    position: absolute;
    top: 0;
    right: 0;
    margin: 0;
    padding: 0;
`